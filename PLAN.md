## مشروع نظام إدارة الوثائق والبحث الدلالي لهيئة التشريع والرأي القانوني

### نظرة عامة
تطبيق ويب مبني بـ Flask يتيح:
- رفع ملفات PDF.
- استخراج النص صفحة بصفحة عبر OCR من Mistral (مع بديل محلي باستخدام pypdf عند عدم توفر OCR).
- توليد تمثيلات دلالية للنص باستخدام Sentence Transformers.
- تنفيذ بحث دلالي سريع باستخدام FAISS.
- واجهة ويب نظيفة باستخدام Bootstrap 5.

### الأهداف
- **تبسيط الرفع والمعالجة**: واجهة رفع واحدة ومعالجة تلقائية للنص والتمثيلات.
- **بحث دلالي**: نتائج ترتّب حسب المعنى، وليس مجرد المطابقة النصية.
- **سهولة النشر**: الاعتماد على SQLite وFAISS في ملفات محلية.

### المكدس التقني
- **Backend**: Flask (Python)
- **Database**: SQLite عبر SQLAlchemy
- **OCR**: Mistral AI (mistralai)
- **Embeddings**: sentence-transformers ('all-MiniLM-L6-v2')
- **Vector Search**: faiss-cpu
- **Frontend**: HTML + Bootstrap 5
- **Env Vars**: python-dotenv

### هيكل المشروع
```text
app.py
requirements.txt
.env
uploads/            # ملفات PDF المرفوعة
 db/                # قاعدة البيانات ومؤشر FAISS
 templates/
  └─ index.html     # واجهة المستخدم
```

### نماذج قاعدة البيانات
- **Document**: `id`, `filename`, `upload_timestamp`
- **Page**: `id`, `document_id` (FK)، `page_number`, `content`

العلاقة: Document 1..N Page.

### تدفق العمل
1) **رفع PDF**:
   - المستخدم يرفع ملفًا عبر `/upload`.
   - يحفظ الملف في `uploads/`.
2) **استخراج النص**:
   - محاولة استخدام OCR من Mistral على كامل الملف (Base64) وإرجاع نص كل صفحة.
   - إن لم يتوفر OCR، يستخدم `pypdf` لاستخراج نص من الصفحات النصية.
3) **تخزين البيانات**:
   - إنشاء سجل `Document`.
   - إنشاء سجلات `Page` لكل صفحة بنصها.
4) **التمثيلات الدلالية وبناء الفهرس**:
   - حساب embedding لكل صفحة (تطبيع المتجهات).
   - إضافة المتجه إلى مؤشر FAISS (IndexFlatIP).
   - تحديث خريطة `index_to_page_id` لحفظ الربط بين موضع المتجه ومعرف الصفحة.
   - حفظ المؤشر والخرائط إلى `db/vector_index.faiss` و`db/index_map.json`.
5) **البحث الدلالي**:
   - في `/search`، يُحسب embedding للاستعلام.
   - يُستعلم FAISS عن أفضل k (مثلاً 5) نتائج.
   - تُجلب بيانات الصفحات المطابقة من SQLite وتعرض في الواجهة.

### التكامل مع Mistral OCR
- حفظ مفتاح API في `.env` تحت `MISTRAL_API_KEY`.
- دالة `extract_text_from_pdf` تحتوي نقطة اتصال يمكن استبدالها باستدعاء OCR الفعلي وفق توثيق Mistral.
- عند غياب المفتاح أو فشل الاستدعاء، يُستخدم بديل محلي (`pypdf`).

### إدارة مؤشر FAISS
- **التحميل عند الإقلاع**: `load_faiss_index()` لإنشاء/قراءة المؤشر والـ mapping.
- **الإضافة**: `add_embedding_to_index(embedding, page_id)`.
- **الحفظ**: `save_faiss_index()` بعد إضافة صفحات جديدة.
- **البحث**: `search_similar(embedding, k)` يعيد الدرجات ومعرّفات الصفحات.

### الواجهة الأمامية
- صفحة واحدة `templates/index.html` تحوي:
  - نموذج رفع ملف PDF.
  - نموذج بحث نصي.
  - قائمة أحدث الملفات المرفوعة.
  - نتائج البحث (اسم الملف، رقم الصفحة، مقتطف نصي، درجة التشابه).
- تصميم RTL عربي باستخدام Bootstrap 5.

### الإعداد والتشغيل محليًا (Windows PowerShell)
1) إنشاء بيئة افتراضية وتثبيت المتطلبات:
```bash
python -m venv .venv
. .venv/Scripts/Activate.ps1
pip install -r requirements.txt
```
2) إعداد `.env`:
```env
MISTRAL_API_KEY="PASTE_YOUR_MISTRAL_API_KEY_HERE"
```
3) تشغيل التطبيق:
```bash
python app.py
```
- افتح المتصفح على `http://127.0.0.1:5000`.

### اعتبارات الأمان والخصوصية
- التحقق من نوع الملف واسم الملف (`secure_filename`).
- تحديد حجم الرفع الأقصى مستقبلًا إذا لزم.
- إدارة مفاتيح البيئة عبر `.env` وعدم رفعها للمستودع.

### تحسينات مستقبلية
- دعم التقسيم الذكي للنص (chunking) بدل الصفحة فقط.
- تمييز النتائج وإبراز المقتطف المطابق (highlighting).
- دعم لغات/نماذج تمثيل أخرى أو نماذج عربية أقوى.
- استخدام قاعدة بيانات متقدمة للفكتورات (مثل pgvector) عند التوسع.
- دعم المعالجة غير المتزامنة لقوائم كبيرة من الملفات.

### الاختبارات والقبول
- رفع ملفات PDF نصية وأخرى ممسوحة ضوئيًا للتحقق من مسار OCR.
- اختبارات وحدة لدوال: استخراج النص، بناء/تحميل FAISS، البحث.
- تحقق يدوي لنتائج البحث عبر استعلامات متنوعة.

### خطة تنفيذ مرحلية (MVP مبسّط)
- المرحلة 1: إعداد Flask، النماذج، القوالب، رفع الملفات، استخراج محلي للنص، تخزين.
- المرحلة 2: embeddings + FAISS + البحث الدلالي.
- المرحلة 3: تكامل OCR مع Mistral وإدارة الأخطاء.
- المرحلة 4: تحسينات الواجهة وتجربة المستخدم.
